package com.algorithms.sorting;

/**
 * TODO: make it work only for strings and convert T to string somehow.
 *
 * @author Dante-666
 * @version 1.0
 */
public class MSDRadixSort<T extends Comparable<T>> extends Sort<T> {

    /**
     * The only constructor that initializes the array.
     *
     * @param array The array to be sorted
     */
    public MSDRadixSort(T[] array) {
        super(array);
    }

    @Override
    public void sortArray() {

    }
}